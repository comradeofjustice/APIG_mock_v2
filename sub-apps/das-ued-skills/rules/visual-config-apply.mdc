---
description: 读取 project_description.md 的 Visual Config 和 Layout Config 节，在代码生成时将维度值映射为 CSS 变量覆盖。与 pencil-token-refresh.mdc（处理颜色+圆角）配合，两者共同完成 project_description.md → 完整项目配置的注入。在生成 Vue 代码、审查样式规范、搭建新页面时自动生效。
globs:
  - "src/**/*.vue"
  - "src/**/*.ts"
alwaysApply: false
---

# 视觉配置应用规则

## 读取方式

每次代码生成前，先读取工作区根目录的 `project_description.md`，定位 `## Visual Config` 和 `## Layout Config` 两节，提取表格中的值，按下方映射表生成 CSS 覆盖。

---

## Visual Config 维度映射

### `bg-tone`（背景色调）

| 值 | `--color-bg-page` | `--color-bg-secondarycontainer` | 适用场景 |
|----|---|---|---|
| `cool`（默认） | `var(--gray-10)` → `#F6F7FB` | `var(--gray-5)` → `#F0F1F5` | 通用，偏冷蓝灰 |
| `warm` | `#F7F5F2` | `#F0EDE9` | 政务、金融类 |
| `tinted` | `var(--brand-0)` → `#EEF5FF` | `#E4EEFF` | 品牌感强的产品 |

根据 `project_description.md § Visual Config` 中的 `bg-tone` 值生成对应 CSS 覆盖。

### `font-base`（字号基准）

| 值 | CSS 覆盖 |
|----|---------|
| `12px` | `--font-size-base: 12px` |
| `13px` | `--font-size-base: 13px` |
| `14px` | `--font-size-base: 14px`（无需覆盖，全局默认即 14px） |

### `font-family`（字体族）

| 值 | CSS 覆盖 |
|----|---------|
| `PingFang SC` | `--font-family: 'PingFang SC', -apple-system, BlinkMacSystemFont, sans-serif` |
| 其他值 | 替换为对应字体名 |

### `spacing-scale`（间距缩放）

| 值 | JS 调用 | 说明 |
|----|---------|------|
| `compact` | `setSpacingScale(0.75)` | 高密度场景 |
| `standard` | `setSpacingScale(1)` | 无需调用，默认即 1 |
| `relaxed` | `setSpacingScale(1.25)` | 宽松阅读型 |

```js
/** 在 main.ts 中按需调用（standard 无需调用） */
function setSpacingScale(scale) {
  const base = { '5xs':2,'4xs':4,'3xs':6,'2xs':8,'xs':10,
    'sm':12,'md':16,'lg':20,'xl':24,'2xl':32,'3xl':40,'4xl':48 };
  const root = document.documentElement;
  Object.entries(base).forEach(([k, v]) => {
    root.style.setProperty(`--spacing-${k}`, `${Math.round(v * scale)}px`);
  });
}
```

### `info-density`（信息密度）

| 值 | HTML 属性 |
|----|---------|
| `high` | `<html data-density="high">` |
| `medium` | `<html data-density="medium">` |
| `low` | `<html data-density="low">` |

在 `main.ts` 中读取 `project_description.md` 的 `info-density` 值后设置：
```ts
document.documentElement.setAttribute('data-density', '<info-density值>')
```

### `radius-style`（圆角风格）

| 值 | `--radius-lg` | `--radius-md` | `--radius-sm` | `--button-radius` |
|----|---|---|---|---|
| `sharp` | `9px` | `6px` | `3px` | `3px` |
| `standard` | `12px` | `8px` | `4px` | `8px` |
| `rounded` | `20px` | `16px` | `8px` | `20px` |

> 圆角 CSS 值从 `project_description.md § Pencil Token Values` 的圆角节读取，本规则不重复覆盖；`pencil-token-refresh.mdc` 已处理 Pencil Variables 注入。

### `layout-type`（布局结构）

| 值 | 说明 | 侧边栏行为 |
|----|------|---------|
| `sidebar-fixed` | 固定宽度侧边栏 | 宽度由 `sidebar-width` 决定 |
| `sidebar-collapsible` | 可收起侧边栏 | 展开 220px / 收起 56px |
| `top-nav` | 顶部导航，无侧边栏 | — |

### `color-mode`（色彩模式）

| 值 | 处理方式 |
|----|---------|
| `light` | 无需额外配置 |
| `dark` | 读取 `b-admin-design-system/theming/dark-mode.md` |
| `both` | 实现模式切换逻辑 |

---

## Layout Config 映射

从 `## Layout Config` 节读取以下变量，用于代码生成：

| 变量 | CSS / 用途 |
|------|-----------|
| `sidebar-width` | `--sidebar-width: {值}px`；`.sidebar { width: var(--sidebar-width) }` |
| `content-padding` | `.page-container { padding: {值}px }` |
| `content-gap` | `.page-container { gap: {值}px }` |
| `card-header-height` | `.card-header { height: {值}px }` |
| `card-header-padding` | `.card-header { padding: {上下}px {左右}px }` |
| `card-radius` | `.content-card { border-radius: {值}px }` |
| `canvas-width` / `canvas-height` | 仅用于 Pencil 设计阶段，代码不使用 |

### 生成 CSS 基础样式（模板）

读取 `project_description.md § Layout Config` 中的值后，每个页面根元素必须包含以下结构（将 `{变量}` 替换为实际读取的值）：

```css
/* 页面容器（每个页面根元素） */
.page-container {
  display: flex;
  flex-direction: column;
  gap: {content-gap}px;
  padding: {content-padding}px;
  min-height: 100%;
  background: var(--color-bg-page);
}

/* 侧边栏 */
.sidebar {
  width: {sidebar-width}px;
  flex-shrink: 0;
}

/* 卡片容器 */
.content-card {
  background: var(--color-bg-container);
  border-radius: {card-radius}px;
  overflow: hidden;
}

/* 卡片标题栏 */
.card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: {card-header-height}px;
  padding: {card-header-padding};
  border-bottom: 1px solid var(--color-component-stroke);
}
```

---

## 代码审查检查项

收到「检查代码是否符合规范」时，先读取 `project_description.md § Layout Config` 和 `§ Visual Config`，然后验证：

- [ ] `.page-container` padding 是否与 `content-padding` 一致？
- [ ] `.page-container` gap 是否与 `content-gap` 一致？
- [ ] `.content-card` border-radius 是否与 `card-radius` 一致？
- [ ] `.card-header` height 是否与 `card-header-height` 一致，padding 是否与 `card-header-padding` 一致？
- [ ] `data-density` 是否设置为 `info-density` 对应值？
- [ ] `--font-size-base` 是否与 `font-base` 一致（或使用全局默认）？

---

## 新项目适配方法

新项目只需修改 `project_description.md § Visual Config` 和 `§ Layout Config` 中的值，本规则自动读取并生成对应 CSS。无需修改本规则文件。

**换项目时的操作清单：**
1. 修改 `project_description.md § Visual Config` 中的维度值（bg-tone / font-base / spacing-scale 等）
2. 修改 `project_description.md § Layout Config` 中的布局值（sidebar-width / content-padding 等）
3. 说"刷新设计系统" → `pencil-token-refresh.mdc` 注入颜色+圆角到 Pencil
4. 代码生成时本规则自动读取新值并生成对应 CSS
