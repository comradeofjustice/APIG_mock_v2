---
description: 当新的 SKILL.md 文件被创建时（即用户下载/安装了新 Skill），自动分析该 Skill 与 ux-to-code 全流程编排的契合度，检测融合价值和决策冲突，输出报告并等待用户拍板。所有处置动作均需用户确认后才执行。
globs:
  - .cursor/skills/*/SKILL.md
alwaysApply: false
---

# 新 Skill 安装分析规则

当 `.cursor/skills/*/SKILL.md` 文件被**新建**时触发（即用户安装了新 Skill）。
若只是打开已有 Skill 进行编辑，先判断是否已注册，已注册则跳过。

---

## Step 1：判断是否是「新 Skill」

先读取注册表：
```
Read: ~/.cursor/skills/SKILLS-INDEX.md
```

检查当前文件的 Skill 名称（取自 frontmatter `name` 字段）是否已在注册表 Layer 1 或 Layer 2 中：

- **已注册** → 跳过本规则，正常处理
- **未注册** → 进入 Step 2 分析

---

## Step 2：读取新 Skill 内容

读取该 SKILL.md 的完整内容，重点提取：
1. `description` — Skill 的功能定位
2. 输入/输出（接受什么，产出什么）
3. 触发词（何时被调用）
4. 是否有文件读写操作
5. 是否与设计/代码/研究/文档中某个环节相关

---

## Step 3：对照流程编排做四项检测

### 检测 A：阶段融合价值

对照 `ux-to-code` 的 7 个阶段（初始化/Stage 0-4），判断新 Skill 是否能补充某个阶段：

| 匹配维度 | 信号 | 建议接入位置 |
|---|---|---|
| 输入研究/竞品/用研相关 | 关键词：research/竞品/用户/旅程/调研 | Stage 0 子工具 |
| 输出叙事/流程/IA 相关 | 关键词：story/flow/IA/架构/场景 | Stage 1 补充 |
| 输出文档/规格/需求相关 | 关键词：spec/PRD/需求/验收 | Stage 2 补充 |
| 操作 Pencil / 设计稿相关 | 关键词：.pen/Pencil/设计/排版/颜色 | Stage 3 子工具或后置 |
| 评估/验证/审查相关 | 关键词：audit/review/check/评估/合规 | Stage 3.5 子工具 |
| 生成/还原/代码相关 | 关键词：code/Vue/component/generate | Stage 4 子工具 |
| 代码质量/鲁棒性相关 | 关键词：harden/error/i18n/edge case | Stage 4 后置 |

### 检测 B：与现有 Layer 1 Skills 的重叠

逐一对比新 Skill 的功能与以下 Layer 1 Skills：
- design-validation（启发式评估/合规检查）
- critique（UX 有效性评估）
- clarify（文案检查）
- harden（代码鲁棒性）
- extract（组件提取）

如果功能高度重叠（> 70%），标记为 **⚠️ 重叠冲突**，建议合并而不是并列调用。

### 检测 C：决策冲突

检查新 Skill 是否与现有 Layer 1 Skills 在以下方面存在不同主张：

- **输出格式**：新 Skill 是否要求对话输出，而现有规范要求写文件？
- **触发时机**：新 Skill 建议在某个 Stage "之前/之后" 执行，是否与现有顺序冲突？
- **设计规范**：新 Skill 是否包含与 `compliance-checklist.md` 相悖的设计建议？
- **代码规范**：新 Skill 是否要求不同的框架/组件库/代码风格？

如发现冲突，标记为 **🔴 决策冲突，需要拍板**。

### 检测 D：Layer 2 适配度

如果以上检测均无强融合价值，评估是否适合作为 Layer 2 语境技能：
- 功能独立，不依赖其他 Skill 的 Handoff
- 用户可主动按需调用
- 与现有 Layer 2 Skills 无明显重叠

---

## Step 4：输出分析报告，等待用户拍板

**所有结论均需用户确认后才执行更新，不自动修改任何文件。**

```
## 新 Skill 检测报告：{skill-name}

**功能摘要**：{一句话概括}

---

### 分析结论

{以下四种之一}

#### ✅ 建议融合到 Layer 1 — Stage {N}
- **接入位置**：{具体位置，如"Stage 3.5 新增默认子工具"}
- **触发条件**：{何时执行}
- **与现有 Skill 的关系**：{互补/替换/并列}
- **若确认融合，将更新**：
  - SKILL.md frontmatter 中 layer 改为 1
  - _1_pipeline/README.md 对应 Stage 分组插入条目
  - _2_contextual/README.md 删除该条目（若存在）
  - SKILLS-INDEX.md Layer 1 表格插入新行
  - ux-to-code/SKILL.md 对应 Stage 引用更新

#### ⚠️ 与现有 Skill 重叠 — 建议合并
- **重叠的 Skill**：{skill-name}
- **重叠程度**：{高/中，描述重叠的功能部分}
- **建议**：{保留哪个/如何合并}

#### 🔴 决策冲突 — 需要拍板
- **冲突描述**：{具体冲突内容}
- **选项 A**：{维持现有规则，该 Skill 按 X 方式使用}
- **选项 B**：{采用新 Skill 规则，需要更新 Y}
- **我的建议**：{推荐哪个选项，理由}

#### 📦 建议注册为 Layer 2 独立 Skill
- **触发词建议**：{推荐的调用词}
- **与现有 Layer 2 Skills 的区别**：{不重叠的差异化价值}
- **若确认注册，将更新**：
  - SKILL.md frontmatter 添加 layer: 2
  - _2_contextual/README.md 对应分类插入条目
  - SKILLS-INDEX.md Layer 2 表格插入新行

---

### 请告知处置决定

1. **融合到 Layer 1** → 确认后我来执行更新（5 个文件）
2. **注册为 Layer 2** → 确认后我来执行更新（3 个文件）
3. **暂不处理** → 保持未注册状态，下次安装新 Skill 时不再重复分析此项
4. **⚠️/🔴 类型**：请选择处置方案，或告知具体意见
```

---

## 执行阶段（用户拍板后）

用户确认处置方案后，按以下步骤执行（每步完成后继续，中途若写入失败立即停止并报告）：

### 执行：融合到 Layer 1

1. 将 SKILL.md frontmatter 中 `layer: 2` 改为 `layer: 1`（或插入 `layer: 1`）
2. 在 `_1_pipeline/README.md` 对应 Stage 分组表格末尾插入新行
3. 若该 Skill 已在 `_2_contextual/README.md` 中，删除其条目
4. 在 `SKILLS-INDEX.md` Layer 1 表格插入新行，更新版本记录
5. 在 `ux-to-code/SKILL.md` 对应 Stage 的引用说明中插入该 Skill
6. 输出："已将 {skill-name} 融合到 Stage {N}，已更新 5 个文件。"

### 执行：注册为 Layer 2

1. 在 SKILL.md frontmatter 中插入 `layer: 2`
2. 在 `_2_contextual/README.md` 对应分类表格末尾插入新行
3. 在 `SKILLS-INDEX.md` Layer 2 表格插入新行，更新版本记录
4. 输出："已将 {skill-name} 注册为 Layer 2 语境 Skill，已更新 3 个文件。"

### 执行：暂不处理

不修改任何文件，仅回复："已记录，{skill-name} 保持未注册状态。"

---

## 执行原则

- 分析应客观具体，避免"可能有帮助"这类模糊表述
- 重叠检测要给出具体的功能重叠描述，不能只说"功能类似"
- 决策冲突必须给出明确的两个选项和推荐意见，不能让用户自己想
- 如果新 Skill 质量明显低于现有 Skill（规范更简陋、覆盖更窄），建议"以现有 Skill 为准"并推荐注册为 Layer 2
- **任何文件变更都在用户明确确认后才执行**
