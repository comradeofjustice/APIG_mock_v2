---
description: 当 b-admin-pencil-design skill 文件发生变更时，自动提示同步合规清单。适用于对设计系统规范进行修改、新增组件规则、更新 Token 约束等场景。
globs:
  - .cursor/skills/b-admin-pencil-design/**/*.md
alwaysApply: false
---

# 合规清单同步规则

当你修改了 `b-admin-pencil-design` 目录下的任何文件后，必须执行以下检查：

## 触发条件

满足以下任一条件时触发：
- 修改了 `core/tokens-variables.md`（Token 数值变化 → 可能影响硬编码禁止规则）
- 修改了 `core/syntax-rules.md`（语法规则变化 → 可能影响合规要求）
- 修改了 `core/components/*.md`（新增/调整组件规范）
- 修改了 `SKILL.md` 中的设计规则描述

## 必须执行的动作

1. **读取当前合规清单**
   ```
   Read: ~/.cursor/skills/b-admin-pencil-design/core/compliance-checklist.md
   ```

2. **判断是否需要更新**
   - 新的规则是否需要加入合规清单？
   - 已有规则的数值/变量名是否发生了变化？
   - 是否有规则已废弃需要移除？

3. **若需要更新**：
   - 修改 `~/.cursor/skills/b-admin-pencil-design/core/compliance-checklist.md`
   - 在"变更记录"表格中追加一行（版本号递增、日期、变更内容）
   - 不需要修改 `design-validation/SKILL.md`（它在运行时动态读取）

4. **若不需要更新**：无需操作。

## 设计原则

- `compliance-checklist.md` 是**唯一数据源**，`design-validation` 运行时从此读取
- 永远不要在 `b-admin-pencil-design/SKILL.md` 或 `design-validation/SKILL.md` 中维护规则副本
- 合规清单只包含**可机械检查**的规则（有明确通过/失败标准），不包含设计哲学或定性描述
